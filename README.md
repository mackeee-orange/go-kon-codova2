# go-kon-codova2


## install
    `$npm install`
    これでnode_modulesがインストールされます

## preset
    `$npm install -g gulp`
    これでgulpコマンド使えるようになります

## build
    `$gulp browserify`
    `$cordova run [platform]`
